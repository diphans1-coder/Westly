[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"
  base = "."

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
